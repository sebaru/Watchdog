
 - To prepare installation :
       [root@host Watchdog]# useradd watchdog -d /var/WatchdogHome
       [root@host Watchdog]# passwd watchdog

       [root@host Watchdog]# urpmi automake make libtool
       [root@host Watchdog]# urpmi bison flex

       [root@host Watchdog]# urpmi gcc-c++
       [root@host Watchdog]# urpmi glib2-devel libgnomeui2-devel gtksourceview-devel
       [root@host Watchdog]# urpmi goocanvas-devel libvlc-devel nut-devel mysql-devel
       [root@host Watchdog]# urpmi gnokii-devel gtkdatabox-devel ungif4-devel

 - To Compile libvlc-gtk :

       [watchdog@host directory]$ svn checkout http://libvlc-gtk.googlecode.com/svn/trunk/ .
       [watchdog@host directory]$ aclocal; libtoolize; autoheader; autoconf; automake --add-missing
       [watchdog@host directory]$ ./configure; make
       [watchdog@host directory]$ su
       Password:
       [root@host directory]# make install
       [root@host directory]# exit

 - To compile and install Watchdog :

       [watchdog@host Watchdog]$ aclocal; libtoolize; autoheader; autoconf; automake --add-missing
       [watchdog@host Watchdog]$ ./configure
       [watchdog@host Watchdog]$ su
       Password:
       [root@host Watchdog]# make install
       [root@host Watchdog]# exit

 - Before running :
       [root@host Watchdog]# urpmi MySQL-server
       [root@host Watchdog]# /usr/bin/mysqladmin -u root password test   ! Change "test" by a reel password
       [root@host Watchdog]# /usr/bin/mysqladmin -u root -p create WatchdogDB
       [root@host Watchdog]# mysql -u root -p WatchdogDB
       Enter password: 
       Welcome to the MySQL monitor.  Commands end with ; or \g.
       mysql> CREATE USER watchdog IDENTIFIED BY 'watchdogpassword';
       Query OK, 0 rows affected (0.00 sec)
       mysql> GRANT SELECT, INSERT, UPDATE, DELETE, EVENT ON WatchdogDB.* TO watchdog;
       mysql> source init_db.sql
       mysql> exit

       [watchdog@host Watchdog]$ Watchdogd --initrsa


